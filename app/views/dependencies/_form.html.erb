<%= form_with(model: dependency, local: true) do |form| %>
  <% if dependency.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dependency.errors.count, "error") %> prohibited this dependency from being saved:</h2>

      <ul>
      <% dependency.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :dependency_name %>
  </div>

  <div class="field">
    <%= form.label :authors %>
    <%= form.text_field :authors, id: :dependency_authors %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
